create a folder called data, inside the data folder make a file called config.json

inside of the config.json add the following:

{
    "token": "",
    "bypassGuilds": "",
    "bypassUsers": "",
    "logChannel": ""
}